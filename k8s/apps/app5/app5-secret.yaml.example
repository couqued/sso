apiVersion: v1
kind: Secret
metadata:
  name: app5-secret
  namespace: sso-pilot
type: Opaque
stringData:
  # Keycloak Admin Console → Clients → app5-client → Credentials 탭에서 복사
  KEYCLOAK_CLIENT_SECRET: REPLACE_WITH_APP5_CLIENT_SECRET
