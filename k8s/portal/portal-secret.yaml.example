apiVersion: v1
kind: Secret
metadata:
  name: portal-secret
  namespace: sso-pilot
type: Opaque
stringData:
  # Keycloak Admin Console → Clients → portal-client → Credentials 탭에서 복사
  KEYCLOAK_CLIENT_SECRET: REPLACE_WITH_PORTAL_CLIENT_SECRET
