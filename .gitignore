# ── K8s Secrets (절대 커밋 금지) ──────────────────────────────
# 실제 secret 값이 담긴 파일. .example 파일을 복사해서 사용할 것.
k8s/**/*-secret.yaml

# ── 빌드 산출물 ───────────────────────────────────────────────
target/
dist/
build/
node_modules/
*.jar
*.class

# ── 환경변수 / 로컬 설정 ──────────────────────────────────────
.env
.env.local
.env.*.local

# ── IDE ───────────────────────────────────────────────────────
.idea/
.vscode/
*.iml

# ── 인증서 / 키 파일 ──────────────────────────────────────────
*.pem
*.key
*.p12
*.jks
*.pfx

# ── OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
